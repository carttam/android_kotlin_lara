<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/border"
    android:orientation="vertical">

    <com.android.volley.toolbox.NetworkImageView
        android:id="@+id/post_image"
        android:layout_width="match_parent"
        android:layout_height="@dimen/image.height"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout_editor_absoluteX="0dp"
        android:scaleType="centerCrop"/>

    <TextView
        android:id="@+id/title_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:padding="@dimen/text.padding"
        android:text="علی رضا"
        android:textDirection="rtl"
        android:textSize="@dimen/text.font_size"
        app:layout_constraintTop_toBottomOf="@+id/post_image"
        tools:layout_editor_absoluteX="0dp" />

    <TextView
        android:id="@+id/description_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:padding="@dimen/text.padding"
        android:text=" عنوان متنعنوان متنعنوان متنعنوان متنعنوان متنعنوان متنعنوان متنعنوان متنعنوان متنعنوان متنعنوان متنعنوان متنعنوان متن عنوان متن "
        android:textDirection="rtl"
        android:textSize="@dimen/text.font_size"
        app:layout_constraintTop_toBottomOf="@+id/title_text_view"
        tools:layout_editor_absoluteX="0dp" />

</androidx.constraintlayout.widget.ConstraintLayout>